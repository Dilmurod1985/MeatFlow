# Example environment variables for MeatFlow
# DATABASE_URL can be sqlite (default) or a full RDBMS URL
# Example (SQLite file in project):
DATABASE_URL=sqlite:///./meatflow.db

# Example (Postgres):
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/meatflow
